import { FormEvent, ReactNode } from 'react'
type Props={title:string; children:ReactNode; onClose:()=>void; onSubmit?:(e:FormEvent<HTMLFormElement>)=>void; submitText?:string}
export default function Modal({title,children,onClose,onSubmit,submitText='저장하기'}:Props){return <div className="modal-backdrop" role="presentation" onMouseDown={onClose}><section className="modal" role="dialog" aria-modal="true" aria-label={title} onMouseDown={e=>e.stopPropagation()}><div className="modal-head"><h2>{title}</h2><button className="close-button" onClick={onClose} aria-label="닫기">×</button></div><form onSubmit={onSubmit}>{children}<div className="modal-actions"><button type="button" className="cancel-button" onClick={onClose}>취소</button>{onSubmit&&<button type="submit" className="save-button">{submitText}</button>}</div></form></section></div>}
